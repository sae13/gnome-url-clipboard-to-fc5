{
  "name": "gnome-url-clipboard-to-fc5",
  "version": "2.0.0",
  "license": "GPL-3.0-or-later",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/urlShortener.test.js",
    "check": "node --check extension.js && node --check urlShortener.js",
    "pack": "gnome-extensions pack --force --extra-source=urlShortener.js . && unzip -t url-shortener@fc5.ir.shell-extension.zip"
  }
}
